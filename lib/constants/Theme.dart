import 'dart:ui' show Color;

class ArgonColors {
  static const Color black = Color(0xFF000000);

  static const Color white = Color(0xFFFFFFFF);

  static const Color verdeClaro = Color.fromRGBO(231, 238, 232, 1.0);

  static const Color verdeOscuro = Color.fromRGBO(
      90, 176, 156, 1.0);

  static const Color celeste = Color.fromRGBO(223, 237, 247, 1.0);

  static const Color azul = Color.fromRGBO(38, 104, 146, 1.0);

}
